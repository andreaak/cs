@*Шаблон для отображения значений свойства типа Role*@
@using _01_ASPMVCTest.Areas._002_View.Models
@model Role

@foreach (Role value in Enum.GetValues(typeof(Role)))
{
    if (value == Model)
    {
        <u>@value</u>@:&nbsp;
    }
    else
    {
        @value @:&nbsp;
    }
}

@*Guest&nbsp;User&nbsp;<u>Admin</u>&nbsp;*@